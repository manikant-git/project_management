How to add to your project:

Save above code as project.test.js

In your project folder, run:

bash
npm init -y
npm install jest --save-dev
Add this to your package.json:

json
"scripts": {
  "test": "jest"
}
Run:

bash
npm test
